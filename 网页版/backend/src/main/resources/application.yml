spring:
  application:
    name: luying-web-backend
  profiles:
    active: dev

server:
  port: 8080

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true

knife4j:
  enable: true

security:
  jwt:
    secret: ${SECURITY_JWT_SECRET:QUJDREVGR0hJSktMTU5PUFFSU1RVVldYWVo0NTY3ODkwQUJDREVGR0g=}
    expiration-hours: ${SECURITY_JWT_EXPIRATION_HOURS:168}
